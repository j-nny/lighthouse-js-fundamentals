const isEven = function (num) {
  return num % 2 === 0; // returns the Boolean value if num is even or not
}

console.log(isEven(10)); //console.log is a function that is pre-defined by JS so it does not have to be defined in this code
console.log(isEven(11));